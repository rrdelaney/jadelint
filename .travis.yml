language: node_js

node_js:
    - stable

sudo: false

before_script:
    - npm install -g grunt-cli

after_success:
    - 'cat ./coverage/lcov.info | ./node_modules/.bin/coveralls'
    - grunt coffee doc
    - sh script/deploy
